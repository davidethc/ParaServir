# INSTRUCCIONES PARA CONFIGURAR .env
# Copia este archivo y renómbralo a .env (sin la extensión .txt)
# Luego completa con tus credenciales reales de PostgreSQL

# Base de Datos PostgreSQL Local
# Formato: postgresql://usuario:contraseña@localhost:puerto/nombre_base_datos
DATABASE_URL=postgresql://postgres:TU_CONTRASEÑA@localhost:5432/paraservir

# O si prefieres usar variables individuales (descomenta y completa):
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=paraservir
# DB_USER=postgres
# DB_PASSWORD=TU_CONTRASEÑA

# JWT Secret (genera uno seguro)
# En terminal ejecuta: openssl rand -base64 32
JWT_SECRET=tu_jwt_secret_super_seguro_aqui_cambiar_esto

# Resend API Key (para emails de verificación)
RESEND_API_KEY=tu_resend_api_key_aqui

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# Node Environment
NODE_ENV=development

# Puerto del servidor
PORT=3900

